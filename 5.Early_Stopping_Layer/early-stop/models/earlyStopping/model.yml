# Early Stopping technique with Layer
#
# Any directory includes an `model.yml` will be treated as a ml model project.
# In this `yaml` file, we will define the attributes of our model.

apiVersion: 1

# Name and description of our model
name: "early stopping"
type: model
description: "Early stopping in Keras"

training:
  name: "early_stopping_keras_training"
  description: "Early stopping in Keras Training"

  # The source model definition file with a `train_model` method
  entrypoint: model.py

  # File includes the required python libraries with their correct versions
  environment: requirements.txt
